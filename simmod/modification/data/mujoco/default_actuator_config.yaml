---
options:
  execution: 'RESET'
gear:
  default:
    values:
      - [0.0, .inf]
ctrlrange:
  default:
    values:
      - [-.Inf, .inf]
      - [-.Inf, .inf]
forcerange:
  default:
    values:
      - [-.Inf, .inf]
      - [-.Inf, .inf]
